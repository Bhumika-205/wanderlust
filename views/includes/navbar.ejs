<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
    <div class="container-fluid px-4">

        <!-- Logo -->
        <a class="navbar-brand me-3" href="/listings">
            <i class="fa-regular fa-compass"></i>
        </a>

        <!-- Search: always visible -->
        <form action="/listings" method="GET" class="search-form d-flex" role="search">
            <div class="search-wrapper d-flex align-items-center">
                <input class="form-control search-input" type="text" name="search"
                        placeholder="Search destinations"
                        value="<%= typeof search !== 'undefined' ? search : '' %>" />
                <button class="btn search-btn" type="submit">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    <span class="search-text">Search</span>
                </button>
            </div>
        </form>

        <!-- Burger (mobile/tablet only) -->
        <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Nav links -->
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav mx-auto d-lg-flex gap-1">
                <a class="nav-link" href="/listings">Explore</a>
                <a class="nav-link" href="/wishlist">Wishlist</a>
                <a class="nav-link" href="/listings/new">Share your Stay</a>
            </div>
            <div class="navbar-nav d-lg-flex align-items-center gap-1 ms-auto">
                <% if(!currUser) { %>
                    <a class="nav-link" href="/signup"><b>Sign up</b></a>
                    <a class="btn btn-outline-dark btn-sm login-btn" href="/login">Log in</a>
                <% } %>
                <% if(currUser) { %>
                    <a class="btn btn-outline-dark btn-sm login-btn" href="/logout">Log out</a>
                <% } %>
            </div>
        </div>

    </div>
</nav>